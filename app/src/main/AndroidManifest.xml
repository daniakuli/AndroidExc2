<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidExc2"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Student List"
            android:theme="@style/Theme.AndroidExc2.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".StudentRecyclerList"
                  android:exported="true"
                  android:label="New Students">
        </activity>
        <activity android:name=".AddStudentActivity"
            android:exported="true"
            android:label="New Students">
        </activity>
        <activity android:name=".StudentShow"
                  android:exported="true"
                  android:label="Students Details">
        </activity>
        <activity android:name=".StudentEdit"
                  android:exported="true"
                  android:label="Edit Students">
        </activity>
    </application>

</manifest>